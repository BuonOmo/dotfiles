# Dotfiles

Install with:

```zsh
zsh $(curl -fsSL https://raw.githubusercontent.com/BuonOmo/dotfiles/main/install.zsh)
```

Update with:

```zsh
dotf pull
```

Contribute with:

```zsh
dotf add .somerc
dotf commit
# fork if needed
dotf push
# PR welcome
```
